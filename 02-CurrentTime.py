# 2 - Current time
# Write a program that prints the current time and date to the console.

import datetime as dt
print (dt.datetime.now().now()) # Adapted from https://docs.python.org/3/library/datetime.html#datetime-objects
